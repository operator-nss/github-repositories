import styled from 'styled-components';

export const Spinner = styled.div`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  img {
    width: 80px;
    height: 80px;
  }
`;
